apiVersion: v1
kind: Service
metadata:
  name: {{ include "kuberay-dashboard.fullname" . }}
  labels:
    {{- include "kuberay-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kuberay-dashboard.selectorLabels" . | nindent 4 }}
